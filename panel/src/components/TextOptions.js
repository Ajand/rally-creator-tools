import {createUseStyles} from 'react-jss'

const useStyles = createUseStyles({
    
  })
  

const Panel = (children) => {
    const classes = useStyles()

    return (
        <div>This is gonna handle Text Options</div>
    )
}

export default Panel