let accessToken = "";
let gotAccessTokenAt = new Date();

const getAccessToken = (username, password) => {
  return accessToken;
  const access_token = "";
  // TODO must implement it
  return access_token;
};

const authorize = (callbackUrl) => {
  // getAccessToken
};


const getUser = (userOauthToken) => {
    return {
       // username,
       // rnbUserId
    }
}

const getBalance = () => {
    return {
        ///
    }
}

